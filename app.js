const numbers = [
    19, 23, 4, 16, 28, 13, 31, 8, 29, 14, 6, 35, 2, 11, 17, 5, 9, 27, 12, 30,
  ];
  if (numbers > 2 && numbers %2 != 0 && numbers % 3 == 1 ){
    let primearray =""
    primearray[] = numbers
  }
  if(primearray[0] > primearray[]){
    console.log(primearray[])
  }
    
else(primearray[0]<primearray){
    console.log(primearray[])
}
